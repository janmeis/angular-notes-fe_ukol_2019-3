import { FbaseDatePipe } from './fbase-date.pipe';

describe('FbaseDatePipe', () => {
  it('create an instance', () => {
    const pipe = new FbaseDatePipe();
    expect(pipe).toBeTruthy();
  });
});
